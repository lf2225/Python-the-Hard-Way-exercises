import Hand
import Deck

#cool way to make 6, or however many players
PlayerHands = [Hand() for i in range (0, 6)]

class Player(object)
	def __init__ (self, PlayerNumber):
		self.PlayerNumber = PlayerNumber
		self.Decision = stay
		self.IfTurn = True
		self.CurrentHand = Hand()



def Turn():
	while Hand.TotalHandValue =< 16:
		NewCard = Deck.DealOneCard()
		self.CurrentHand.PlayerHand.append(NewCard)
		CurrentHand.TotalHandValue += Hand.ValueDict[NewCard[0]]
	else


Hand =

Money = 

#based on the running sum, stand/hit

